% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_linear_selection.R
\name{analyze_linear_selection}
\alias{analyze_linear_selection}
\title{Analyze linear selection gradients (beta)}
\usage{
analyze_linear_selection(
  data,
  fitness_col,
  trait_cols,
  fitness_type,
  binary_response_col = NULL
)
}
\arguments{
\item{data}{A data frame containing fitness and trait measurements.}

\item{fitness_col}{A string specifying the response column for the OLS gradient model (relative fitness).}

\item{trait_cols}{A character vector of trait column names.}

\item{fitness_type}{A string indicating the fitness type: \code{"binary"}, \code{"continuous"}, \code{"count"}, or \code{"proportion"}.}

\item{binary_response_col}{Optional string naming the raw fitness column (0/1 for binary, counts for count fitness) used for the GLM that supplies p-values. If \code{NULL}, \code{fitness_col} is treated as the raw outcome and relativised internally.}
}
\value{
A list containing the fitted models, summaries, ANOVA tables, and VIFs.
}
\description{
Estimates linear selection gradients by OLS on relative fitness. Binary
fitness gets its p-values from a logistic GLM and count fitness from a
Poisson GLM (negative binomial if overdispersed), since the OLS tests are not
valid for either.
}
\examples{
prep <- prepare_selection_data(bumpus, "survival", c("total_length", "weight"))
fit <- analyze_linear_selection(prep, "survival", c("total_length", "weight"), "binary")
extract_linear_coefficients(c("total_length", "weight"), fit)
}
