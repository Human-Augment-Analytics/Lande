% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adaptive_landscape.R
\name{adaptive_landscape}
\alias{adaptive_landscape}
\title{Calculate Adaptive Landscape}
\usage{
adaptive_landscape(
  data,
  fitness_model,
  trait_cols,
  group_col = NULL,
  population_variance = NULL,
  simulation_n = 1000,
  grid_n = 50,
  custom_range = NULL
)
}
\arguments{
\item{data}{A data frame containing the original trait and fitness data.}

\item{fitness_model}{A fitted model object (e.g., GAM or Tps) predicting individual fitness.}

\item{trait_cols}{One or two trait column names. With one trait the model
is usually the \code{$model} of \code{univariate_spline()} and the result
is a curve; with two it is the \code{$model} of
\code{correlated_fitness_surface()} and the result is a surface.}

\item{group_col}{Optional character string specifying a grouping variable.}

\item{population_variance}{Optional covariance matrix for the traits. Estimated from data if \code{NULL}.}

\item{simulation_n}{Integer specifying the number of individuals to simulate per grid point. Default is 1000.}

\item{grid_n}{Integer specifying the resolution of the population mean grid. Default is 50.}

\item{custom_range}{Optional list specifying custom ranges for the traits.}
}
\value{
An object of class \code{"adaptive_landscape"}.
}
\description{
Computes the adaptive landscape (mean fitness as a function of population mean phenotype)
using a fitted individual-level fitness model.
}
\details{
For a single trait the grid also carries \code{.ind_fit}, the
  individual fitness function evaluated at each population mean, so the two
  curves can be drawn together (see \code{plot_adaptive_landscape()}).
}
