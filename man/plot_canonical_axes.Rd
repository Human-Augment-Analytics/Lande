% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonical_analysis.R
\name{plot_canonical_axes}
\alias{plot_canonical_axes}
\title{Fitness along the canonical axes}
\usage{
plot_canonical_axes(ca, which = 1:2, grid_n = 40, ...)
}
\arguments{
\item{ca}{Output of \code{canonical_analysis()}.}

\item{which}{One or two axis numbers. Default is \code{1:2}.}

\item{grid_n}{Grid resolution of the surface. Default is 40.}

\item{...}{Passed to \code{plot_correlated_fitness()} for two axes or to
\code{plot_univariate_fitness()} for one.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the fitness surface in the space of two canonical axes, or the
fitness function along one, from the scores of \code{canonical_analysis()}.
A negative \eqn{\lambda} is stabilising selection along an axis only when
fitness along that axis has a peak inside the data, otherwise it is
only curvature.
}
\examples{
ca <- canonical_analysis(bumpus, "survival", c("total_length", "weight", "humerus"))
plot_canonical_axes(ca, which = c(1, 3), grid_n = 25)
}
