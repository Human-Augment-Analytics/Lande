% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selection_report.R
\name{selection_report}
\alias{selection_report}
\title{Standardised selection analysis report}
\usage{
selection_report(
  data,
  fitness_col,
  trait_cols,
  fitness_type = c("auto", "binary", "continuous"),
  standardize = TRUE,
  group = NULL,
  use_relative_for_fit = TRUE,
  include_differentials = TRUE,
  digits = 4
)
}
\arguments{
\item{data}{A data frame containing fitness and trait measurements.}

\item{fitness_col}{A string specifying the name of the fitness column.}

\item{trait_cols}{A character vector of trait column names.}

\item{fitness_type}{A string indicating the fitness type: \code{"auto"}, \code{"binary"}, or \code{"continuous"}.}

\item{standardize}{Logical indicating whether to standardize traits to mean 0 and SD 1. Default is \code{TRUE}.}

\item{group}{Optional string specifying a grouping variable (e.g., "year", "site").}

\item{use_relative_for_fit}{Logical; if \code{TRUE} (default) the gradients are estimated on relative fitness \eqn{W / \bar{W}} for every fitness type, as in Lande & Arnold (1983). Set \code{FALSE} only to reproduce coefficients on the absolute fitness scale.}

\item{include_differentials}{Logical; if \code{TRUE} (default) selection
differentials S are included alongside the gradients.}

\item{digits}{Integer number of digits used when printing. Default is 4.}
}
\value{
A data frame of class \code{"selection_report"} with columns
  \code{Term}, \code{Type}, \code{Estimate}, \code{Std_Error}, and
  \code{P_Value}.
}
\description{
Runs the standard Lande-Arnold workflow and returns one tidy table combining
selection differentials (S) and linear (beta), quadratic (gamma), and
correlational (gamma_ij) gradients. All estimates use traits standardised to
mean 0, SD 1 and relative fitness, so the table is directly comparable across
studies.
}
\details{
Differentials and gradients are computed on the same individuals
  (those with complete fitness and trait values) and on the same trait and
  fitness scales, so the rows are directly comparable. S is the
  population covariance (divides by n) while the OLS gradient on
  sd-standardised traits corresponds to the sample covariance (n - 1), so
  for a single trait the Differential and Linear rows differ by the factor
  (n - 1) / n.
}
\examples{
\dontrun{
selection_report(my_data, "fitness", c("trait1", "trait2"))
}
}
