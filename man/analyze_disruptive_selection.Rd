% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_disruptive_selection.R
\name{analyze_disruptive_selection}
\alias{analyze_disruptive_selection}
\title{Analyze disruptive/stabilizing selection}
\usage{
analyze_disruptive_selection(
  data,
  fitness_col,
  trait_col,
  fitness_type = c("binary", "continuous"),
  standardize = TRUE,
  group = NULL
)
}
\arguments{
\item{data}{A data frame containing fitness and trait measurements.}

\item{fitness_col}{A string specifying the name of the fitness column.}

\item{trait_col}{A string specifying the name of the single trait column.}

\item{fitness_type}{A string indicating the fitness type: \code{"binary"} or \code{"continuous"}.}

\item{standardize}{Logical indicating whether to standardize the trait to mean 0 and SD 1. Default is \code{TRUE}.}

\item{group}{Optional string specifying a grouping variable; standardisation and
relative fitness are then computed within each group.}
}
\value{
A data frame with one row per gradient (\code{Term}, \code{Type},
  \code{Beta_Coefficient}, \code{Standard_Error}, \code{P_Value}, \code{Variance}).
}
\description{
Detects disruptive or stabilizing selection on a single trait by estimating its
linear (beta) and quadratic (gamma) selection gradients. A positive gamma indicates
disruptive selection; a negative gamma indicates stabilizing selection.
}
